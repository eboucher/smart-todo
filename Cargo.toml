[package]
name = "smart-todo"
version = "0.1.0"
edition = "2021"

[dependencies]
# CLI argument parsing
clap = { version = "4.4", features = ["derive"] }
# Data serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
# Terminal colors
colored = "2.0"
